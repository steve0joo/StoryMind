# StoryMind Backend Dependencies
# Python 3.10+ REQUIRED (recommended: 3.11+)
# Tested with: Python 3.11.9

# All packages tested and verified working

# ============================================================================
# Core Framework
# ============================================================================
flask==3.0.3                    # Web framework for REST API
flask-cors==5.0.0               # CORS support for frontend communication

# ============================================================================
# LangChain (Selective Use - Document Processing Only)
# ============================================================================
langchain==0.3.7                # Document loading and text splitting
langchain-community==0.3.7      # Community loaders (PyPDF, UnstructuredPDF)
langchain-google-genai==2.0.4   # Gemini API wrapper for LangChain
unstructured==0.16.1            # Document parsing library
pypdf==5.1.0                    # PDF processing

# ============================================================================
# RAG & Embeddings (Custom FAISS Implementation)
# ============================================================================
faiss-cpu==1.9.0                # Vector similarity search (CPU version)
sentence-transformers==3.3.1    # Embedding model (all-MiniLM-L6-v2, 384 dims)

# ============================================================================
# Google Cloud AI/ML APIs
# ============================================================================
google-generativeai==0.8.3      # Gemini API for character extraction
google-cloud-aiplatform==1.71.1 # Vertex AI platform for Imagen 3
vertexai==1.71.1                # Vertex AI Python SDK

# ============================================================================
# Database & ORM
# ============================================================================
sqlalchemy==2.0.35              # ORM for SQLite database

# ============================================================================
# Utilities
# ============================================================================
python-dotenv==1.0.1            # Environment variable management
pydantic==2.10.3                # Data validation
numpy>=1.24.0,<2.0.0            # Numerical operations (compatible with LangChain)
requests==2.32.3                # HTTP library
urllib3>=2.0.0,<3.0.0           # HTTP client library (fixes OpenSSL warning)
importlib-metadata>=6.0.0       # Fixes importlib.metadata error
pillow==10.4.0                  # Image processing for placeholder generation

# ============================================================================
# Development & Testing
# ============================================================================
pytest==7.4.0                   # Testing framework

# ============================================================================
# INSTALLATION NOTES:
# ============================================================================
# 1. Create virtual environment (optional but recommended):
#    python3 -m venv venv
#    source venv/bin/activate  # On Windows: venv\Scripts\activate
#
# 2. Install all dependencies:
#    pip install -r requirements.txt
#
# 3. Expected installation time: 3-5 minutes
#
# 4. Total packages installed: ~50+ (including dependencies)
#
# 5. Disk space required: ~500 MB
#
# VERIFIED WORKING ON:
# - Python 3.11.9 (recommended)
# - Python 3.10+ (minimum)
# - macOS (M1/M2 and Intel)
# - Tested: November 9, 2025
#
# UPGRADE FROM PYTHON 3.9:
# See PYTHON_UPGRADE_GUIDE.md for migration instructions
# ============================================================================  